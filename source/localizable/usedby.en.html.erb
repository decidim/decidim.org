---
title: Social Contract | Decidim
description:
---
<div class="container container-vertical">
  <h1 class="page-title">These cities, regions and organizations are already using Decidim</h1>
 <% @cities = data.cities.select {|city| city[:type] == "city"} %>
 <% @regions = data.cities.select {|city| city[:type] == "region"} %>
 <% @orgs = data.cities.select {|city| city[:type] == "org"} %>
    <h2 class="logos-subsection__title">Cities</h2>
    <div class="logos-container">
      <% @cities.each do |use| %>
        <a href="<%= use.url %>" target="_blank" class="logo-item">
          <img src="<%= use.image %>" alt="<%= use.name %>" />
        </a>
      <% end %>
    </div>
    <h2 class="logos-subsection__title">Regions</h2>
    <div class="logos-container">
      <% @regions.each do |use| %>
        <a href="<%= use.url %>" target="_blank" class="logo-item">
          <img src="<%= use.image %>" alt="<%= use.name %>" />
        </a>
      <% end %>
    </div>
    <h2 class="logos-subsection__title">Organizations</h2>
    <div class="logos-container">
      <% @orgs.each do |use| %>
        <a href="<%= use.url %>" target="_blank" class="logo-item">
          <img src="<%= use.image %>" alt="<%= use.name %>" />
        </a>
      <% end %>
    </div>
</div>
