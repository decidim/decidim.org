<div class="flex items-start md:space-x-6 py-6 opacity-100 transition <%= classes ||= '' %>" data-filter-target>
  <div class="md:w-4/5 flex flex-col space-y-2">
    <h6 class="h6"><%= item.title %></h6>
    <p class="par-sm text-gray-500"><%= item.description %></p>
    <a href="<%= item.url %>" class="inline-block par-sm text-red-500"><%= t("modules.more_info") %></a>
  </div>
  <% if item.image %>
    <div class="hidden md:block md:pl-10 md:w-1/5 rounded-xl overflow-hidden">
      <img src="<%= item.image %>" alt="<%= item.title %>" resize="150x150">
    </div>
  <% end %>
</div>
