title: LDAP
url: 'https://github.com/diputacioBCN/decidim-diba/tree/master/decidim-ldap'
image: /uploads/module-ldap.png
description: >-
  A decidim package to provide user authentication via LDAP. Once is active for
  an organization, it is imposible to signup. Adds a new tab in the Decidim
  System admin panel that allows to create LDAP configurations for every
  organization. Different config parameters can be configured for every
  organization.
type: auth
